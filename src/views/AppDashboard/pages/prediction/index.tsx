import { FC } from 'react';

import './prediction.styles.scss';

const Prediction: FC = () => {
    return (
        <section id='prediction'>
            <p>hello there</p>
        </section>
    );
};

export default Prediction;