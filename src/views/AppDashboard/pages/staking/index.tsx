import { FC } from 'react';

import './staking.styles.scss';

const Staking: FC = () => {
    return (
        <section id='staking'>
            <p>hello there</p>
        </section>
    );
};

export default Staking;